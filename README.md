# create-my-own-linux-shell

this shell is a while loop that is true as long as the shell is running or the user types exit as his command.
u must compile this file by "gcc -lreadline myshell.c" command.
